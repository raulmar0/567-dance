{
  "name": "567-dance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "mobile:ios": "rm -r www && tsc && vite build && mv dist www && cap sync ios && cap open ios",
    "mobile:android": "rm -r www && tsc && vite build && mv dist www && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor-community/text-to-speech": "^2.0.1",
    "@capacitor/android": "^4.3.0",
    "@capacitor/core": "^4.3.0",
    "@capacitor/ios": "^4.3.0",
    "howler": "^2.2.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^4.3.0",
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.1.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  }
}
